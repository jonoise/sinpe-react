import React from 'react';

const SinpeModal = () => {
  console.log('recreate!');
  return (
    <div
      style={{
        position: 'absolute',
        width: '500px',
        height: '500px',
        background: 'red',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      🎉
    </div>
  );
};

export default SinpeModal;
